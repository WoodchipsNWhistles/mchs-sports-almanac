---
layout: base.njk
title: lwbb Career Leaders
---

<h1>lwbb Career Leaders</h1>

<p class="lede-small">
Career totals are aggregated from available per-game records. Due to inconsistent record keeping in earlier eras, some historical data may be incomplete. Season data from 2011–12, 2012–13, and 2014–15 through 2024–25 has been verified against official sources, including the KHSAA/Riherds repository, as last reviewed by this project on or about 2026-01-20.
</p>

{% macro playerCell(row) %}
  <a href="{{ playerLinkLookup[row.playerID] }}">
    {{ playerNameLookup[row.playerID] or row.playerID }}
  </a>
{% endmacro %}

{% macro pctCell(x) %}
  {% if x is not none %}
    {{ (x * 100) | round(1) }}%
  {% else %}
    —
  {% endif %}
{% endmacro %}

{% macro rateCell(x) %}
  {% if x is not none %}
    {{ x | round(1) }}
  {% else %}
    —
  {% endif %}
{% endmacro %}

<details open>
  <summary><strong>Scoring</strong></summary>

  <h2>Career Points</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>PTS</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerPtsTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.pts }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>Points Per Game <span class="muted">(min 30 GP)</span></h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>PPG</th><th>PTS</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerPPGTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ rateCell(row.ppg) }}</td>
        <td>{{ row.pts }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</details>

<details>
  <summary><strong>Rebounding & Misc.</strong></summary>

  <h2>Career Rebounds</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>REB</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerRebTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.reb }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>Rebounds Per Game <span class="muted">(min 30 GP)</span></h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>RPG</th><th>REB</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerRPGTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ rateCell(row.rpg) }}</td>
        <td>{{ row.reb }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>Career Double-Doubles</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>DD</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerDDTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.doubleDoubles }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</details>

<details>
  <summary><strong>Shooting Volume</strong></summary>

  <h2>3PT Made</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>3PM</th><th>3PA</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareer3PMTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.threePM }}</td>
        <td>{{ row.threePA }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>3PT Attempts</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>3PA</th><th>3PM</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareer3PATop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.threePA }}</td>
        <td>{{ row.threePM }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>FT Made</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>FTM</th><th>FTA</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerFTMTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.ftM }}</td>
        <td>{{ row.ftA }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>FT Attempts</h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>GP</th><th>FTA</th><th>FTM</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerFTATop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.gp }}</td>
        <td>{{ row.ftA }}</td>
        <td>{{ row.ftM }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</details>

<details>
  <summary><strong>Shooting Percentages</strong> <span class="muted">(minimum attempts)</span></summary>

  <h2>FG% <span class="muted">(min 150 FGA)</span></h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>FG</th><th>FG%</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerFGPctTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.fgM }}-{{ row.fgA }}</td>
        <td>{{ pctCell(row.fgPct) }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>3P% <span class="muted">(min 60 3PA)</span></h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>3PT</th><th>3P%</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareer3PPctTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.threePM }}-{{ row.threePA }}</td>
        <td>{{ pctCell(row.threePct) }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <h2>FT% <span class="muted">(min 50 FTA)</span></h2>
  <table>
    <thead>
      <tr><th>Rk</th><th>Player</th><th>FT</th><th>FT%</th></tr>
    </thead>
    <tbody>
    {% for row in lwbbCareerFTPctTop10 %}
      <tr>
        <td>{{ loop.index }}</td>
        <td>{{ playerCell(row) }}</td>
        <td>{{ row.ftM }}-{{ row.ftA }}</td>
        <td>{{ pctCell(row.ftPct) }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</details>
